# Agent Instructions

- Use `sbtn --client` instead of `sbt`
- Whenever a file is changed, compile only that file with related files using: `sbtn --client "compile --changed"`
- Commit after each change
