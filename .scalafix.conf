# Note: Using DisableSyntax as a lightweight syntactic rule (doesn't require semanticdb).
# OrganizeImports and ProcedureSyntax are disabled due to Scala 3.7.0 compatibility issues.
# Code formatting and import organization are handled by scalafmt instead.
rules = [
  DisableSyntax
]
